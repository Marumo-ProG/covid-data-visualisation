import Typography from "@mui/material/Typography";
import Button from "@mui/material/Button";

const Landing = () => {
    return (
        <div>
            <Typography variant="h1">Landing</Typography>
            <Button color="primary">Lenny</Button>
        </div>
    );
};

export default Landing;
